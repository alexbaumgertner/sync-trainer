# Живой прогон R3, 20 сентября 2026

Что проверял владелец на боевом сразу после выкатки R3, что из этого
оказалось поломкой и что с ней сделано. **Следующая сессия читает этот файл
вместе с `open-items.md`:** здесь то, чем кончился первый настоящий проход
по новому порядку работы.

Все правки выкачены в `main` и на `booth.growtomiddle.dev`. Три коммита:
`509e3de`, `13b292f`, `28ed5b3`.

---

## Восемь сообщений и что за ними стояло

| № | Что увидел человек | Что это было | Проверено |
|---|---|---|---|
| 1 | HTTP 413 при загрузке списка участников | Предел тела запроса Vercel — 4.5 МБ. Файл 5.3 МБ не доходил до кода | Прогнан настоящий файл владельца на боевом |
| 2 | Список терминов захлопывался после каждой правки | `<details>` — состояние разметки, переход его сбрасывает | `?glossary=open` на боевом раскрывает редактор |
| 3 | Не видно, кто завёл и кто правил термин | Авторство показывалось только внутри раскрытой карточки | «завёл: модель» видно в строке на боевом |
| 4 | Непонятно, что за озвучкой надо уйти на другую страницу | Ссылка стояла в шапке списка файлов и читалась как ссылка на файлы | Закрыто вместе с №7 |
| 5 | В скрипте не тот глоссарий | **Регрессия R3-3.** После разделения шагов модель возвращает только НОВЫЕ термины, а файлы собирались из её ответа | Сквозной тест; на боевом требует перегенерации, см. ниже |
| 6 | Темп по умолчанию 105% | Ускорение, которого никто не просил | Сквозной тест |
| 7 | Озвучка требует перехода на другую страницу | — | Сквозной тест |
| 8 | Подсветка убегает вперёд на две фразы | Границы фраз внутри реплики раскладывались по длине текста | На боевом: все 79 границ стоят внутри настоящих пауз |

---

## Про подсветку подробно (№8)

Самая содержательная из находок, и диагноз оказался не тем, что ожидался.

**Чего НЕ было.** Длительности точны: сумма кадров MP3 сходится и с записью
в базе (683,52 с), и с тем, что декодирует браузер. Карта времени помечена
как точная и кончается ровно там же. Расхождения между файлом и картой нет.

**Что было.** Границы реплик точны — они известны по кадрам. А фразы ВНУТРИ
реплики раскладывались пропорционально числу символов. Допущение «речь идёт
с постоянным числом символов в секунду» на настоящей озвучке не держится:
замерено на файле владельца, между голосами разброс от 14,5 до 25,1 симв/с,
в 1,7 раза. На реплике в минуту это и даёт десяток секунд.

**Что не помогло.** Слоги вместо символов. Проверено на тех же данных:
средняя ошибка предсказания стала хуже (9,2% против 8,4%). Добавление пауз
за предложение и запятую — тоже хуже (9,5%). Гипотеза про цифры не подошла:
в скрипте числа записаны словами.

**Что сделано.** Границы притягиваются к настоящим паузам в звуке
(`src/lib/pauses.ts` находит их по среднеквадратичной громкости,
`alignToPauses` в `cues.ts` раскладывает). Синтез не тронут — владелец
выбрал естественность звука против точности до миллисекунды.

Пауз втрое больше, чем предложений: синтез делает их и на запятых. Поэтому
не «ближайшая к каждой границе» — жадный выбор ставил границы вразнобой и
однажды переставил их местами, дав фразу отрицательной длины. Раскладка
считается целиком, динамическим программированием.

Готовые озвучки пересчитываются один раз при первом обращении к карте и
сохраняются с отметкой `cuesAligned`: платить за новый синтез ради
исправления подсветки человек не должен.

---

## Два файла, на которых тестировали

Оба лежат у владельца в `~/Documents`, в репозиторий не копировались.

- **Conference Leadership & Speakers Directory.pdf**, 5.3 МБ, 3 страницы.
  Тот самый 413. После правки разбирается: шесть человек с организациями и
  произношением («фи-ЛИП ма-рам-БО»). Шесть — это ВСЕ, кто в документе есть:
  проверено контрольным вопросом той же модели.
- **ImpactReport25_Booklet-web.pdf**, 15 МБ, 28 страниц. Проходит целиком:
  в проекте 7 обе сборки глоссария по нему завершились без ошибок.
  Опасение, что 15 МБ не влезут в запрос к модели, **не подтвердилось** —
  и это стоит запомнить: оно было выведено арифметикой, а не прогоном.

---

## Что осталось открытым

### Файл «Глоссарий CSV» устаревает

Правка №5 чинит момент генерации: теперь в файл идёт глоссарий проекта,
а не ответ модели. Но файл остаётся **снимком**, и любая правка термина
после генерации делает его неверным. Замерено на боевом 20 сентября:

| Проект | Строк в файле | Терминов в глоссарии |
|---|---|---|
| 7 | 9 | 90 |
| 5 | 45 | 77 |
| 3 | 41 | 66 |
| 1 | 41 | 42 |

Проект 7 показывает старую поломку (9 из 90), остальные — обычное
устаревание: термины добавлялись после генерации, из разборов и руками.

Рядом, в секции глоссария, есть живая выгрузка (CSV и XLSX) — она всегда
верна и знает про слои, варианты и статусы. То есть снимок не нужен никому,
кроме одного: к нему привязана оценка материала (`ratings`, цель
`glossary`). Развязать это — отдельная задача, и решать её владельцу:
убрать снимок из списка файлов совсем или оставить с честной подписью
«на момент генерации».

**Существующие проекты файл не перепишут сами.** Чтобы он стал верным,
нужна перегенерация скрипта.

### Имена участников хранятся со степенями

«Steven W. Hetts, MD, FACR». Для карточки произношения это лишнее —
произносят «Стивен Хеттс». Но в списке участников степени помогают понять,
кто есть кто, поэтому выносить их в отдельное поле без решения владельца
не стал.

### Главное: список багов от ПЕРВОГО живого пользователя так и не прозвучал

`open-items.md` с самого начала называл это самым важным пунктом. За эту
сессию его место заняли восемь находок владельца — они были из собственного
тестирования, а не из того списка. Список по-прежнему у владельца.

---

## С чего начинать следующий прогон

1. Перегенерировать скрипт в проектах, где файл глоссария нужен верным.
2. Открыть проигрыватель и послушать: подсветка должна стоять на фразах
   без ухода вперёд. Пересчёт произойдёт сам при первом обращении.
3. Загрузить список участников — тот же файл на 5.3 МБ или больше.
4. Пройти порядок целиком на новом проекте: материалы → глоссарий →
   выверка → скрипт → озвучка, не уходя со страницы проекта.
