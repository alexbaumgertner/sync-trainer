# R1 — задачи

Рабочий файл. **Агент читает его перед началом задачи и обновляет статус в конце.**
Требования — в [r1-spec.md](./r1-spec.md).

## Как с этим работать

Одна задача — одна сессия агента. Не давайте агенту «сделай R1»: на пятинедельной
задаче без разбиения любая модель и любой инструмент уплывут. Порядок действий:

1. Взять первую задачу со статусом `todo`, у которой все зависимости `done`.
2. Прочитать её критерии приёмки и требования, на которые она ссылается.
3. Сделать, прогнать `npm run build`, `npx eslint src --max-warnings=0`, `npm run smoke`.
4. Закоммитить.
5. Поставить статус `done` и записать, что оказалось не так, как предполагалось.

Статусы: `todo` · `doing` · `done` · `cut`

---

## Фаза 0 — фундамент

Распараллелить нельзя: всё остальное стоит на этом.

### T01 · Postgres, схема, слой доступа
**Статус:** todo · **Зависит от:** — · **Требования:** D1, D3

- Neon Postgres через маркетплейс Vercel, переменные подставляются автоматически
- Схема из спецификации, миграции в репозитории
- Слой доступа, в котором проверка владения проектом выполняется централизованно

**Приёмка:** запрос к чужому проекту возвращает «не найдено», а не чужие данные.
Тест на это существует.

### T02 · Вход по одноразовому коду
**Статус:** todo · **Зависит от:** T01 · **Требования:** A1–A7

- `APP_PASSWORD` удаляется, вместе с ним старый экран входа
- Отправка писем через Resend, отправитель на своём домене
- Ограничители в базе, не в памяти процесса
- Список приглашённых адресов

**Приёмка:** неизвестный адрес получает тот же ответ, что известный. Шестая попытка
кода отклоняется. Четвёртое письмо за 15 минут не отправляется. Адрес не из списка
приглашённых не может войти.

### T03 · Расходы по пользователям
**Статус:** todo · **Зависит от:** T01, T02 · **Требования:** B1–B3, D2

- Перенос накопленного счётчика из Blob в `usage_events` одноразовым скриптом
- Третье условие в `budgetBlock()`: месячный лимит на пользователя
- Отображение расхода на проекте, в профиле и глобально

**Приёмка:** суммы после миграции совпадают с теми, что были в Blob. Пользователь
с исчерпанным личным лимитом получает 402, остальные работают.

---

## Фаза 1 — проекты

### T04 · Проекты: создание, список, карточка
**Статус:** todo · **Зависит от:** T02 · **Требования:** U1

- Список с названием, событием, датой, языком, статусом, стоимостью
- Создание: название, событие, дата, место, язык речи, пресет
- Карточка со всеми файлами проекта
- Пустое состояние объясняет, с чего начать

**Приёмка:** проект создаётся, открывается, удаляется. Удаление уносит артефакты
из Blob (F4), а не только строки.

### T05 · Загрузка документа и удаление оригинала
**Статус:** todo · **Зависит от:** T04 · **Требования:** F1–F4

- PDF, DOCX, PPTX до 25 МБ
- PDF уходит в Gemini напрямую; DOCX и PPTX раскладываются в текст библиотеками
- Оригинал удаляется, остаются метаданные и `purged_at`
- Извлечённый текст нигде не сохраняется

**Приёмка:** после генерации оригинала нет ни в Blob, ни в базе. Извлечённый текст
не встречается ни в одной таблице. При ошибке обработки оригинал тоже удалён.

---

## Фаза 2 — генерация

### T06 · Пресеты стилистики
**Статус:** todo · **Зависит от:** — · **Требования:** G1

- Структура пресета: регистр и синтаксис, роли спикеров, разрешённая и запрещённая
  процедурная лексика, источник терминологии, плотность статистики
- Пресеты живут в репозитории, не в базе: это код, который правят с ревью
- ООН/EN — полный. Остальные помечены черновыми

**Приёмка:** добавление нового пресета не требует правок в коде генерации.

### T07 · Генерация скрипта
**Статус:** todo · **Зависит от:** T05, T06 · **Требования:** G1, G3, G4

- Структурированный ответ по схеме: `{title, speakers[], segments[], ssml, glossary[]}`
- Параметры: длительность, число спикеров, плотность терминов, ловушки, темп
- Скрипт редактируется вручную перед синтезом

**Приёмка:** десять генераций подряд разбираются без единой ошибки парсинга.

### T08 · Синтез аудио
**Статус:** todo · **Зависит от:** T07 · **Требования:** G2, G5, G6

- Валидация SSML до отправки
- Существующий конвейер синтеза переиспользуется без изменений
- Голоса фильтруются по языку проекта
- Пометка синтетического материала в интерфейсе и в файле

**Приёмка:** `npm run smoke` проходит. Скрипт с битым SSML отклоняется до обращения
к Google, а не после.

---

## Фаза 3 — глоссарий и разбор

### T09 · Глоссарий
**Статус:** todo · **Зависит от:** T07 · **Требования:** D3, U2

- Хранение с происхождением: кто предложил, кто подтвердил, когда
- Экспорт в CSV и в формат InterpretBank

**Приёмка:** выгруженный файл открывается в InterpretBank без правок.

### T10 · Разбор после события
**Статус:** todo · **Зависит от:** T09 · **Требования:** E1–E3

- Отметка «прозвучало на событии» у каждого термина
- Недостающие термины попадают в глоссарий со статусом «из практики»
- Короткие вопросы о темпе и совпадении с реальностью

**Приёмка:** отметки сохраняются в `occurred_at_event` и видны при следующем открытии.

### T11 · Обратная связь в генерацию
**Статус:** todo · **Зависит от:** T10 · **Требования:** E4

- Разборы прошлых проектов пользователя подмешиваются в промт следующей генерации

**Приёмка:** два проекта одного пользователя с разными разборами дают заметно
разные скрипты при одинаковых прочих параметрах.

---

## Фаза 4 — отделка

### T12 · Фоновая генерация
**Статус:** todo · **Зависит от:** T08 · **Требования:** U3

**Приёмка:** страницу можно закрыть во время синтеза и вернуться к готовому файлу.

### T13 · Черновые пресеты DE, FR, TR
**Статус:** todo · **Зависит от:** T06, T08 · **Требования:** G6

Перед началом проверить, какие голоса Google даёт для `tr-TR` и поддерживают ли
они SSML: у Chirp 3 HD покрытие языков отличается от Neural2.

**Приёмка:** генерация на каждом языке даёт валидное аудио. Качество регистра
не гарантируется до проверки носителем — так и помечено в интерфейсе.

### T14 · Английский интерфейс
**Статус:** todo · **Зависит от:** T04 · **Требования:** U4 · **кандидат на R1.5**

### T15 · Напоминание о разборе
**Статус:** todo · **Зависит от:** T10 · **Требования:** E5 · **кандидат на R1.5**

---

## Журнал решений

Сюда — то, что выяснилось в работе и противоречит спецификации.
Спецификация правится следом, иначе она устареет к третьей неделе.

| Дата | Задача | Что оказалось не так |
|---|---|---|
| | | |
