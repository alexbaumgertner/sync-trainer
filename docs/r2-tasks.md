# R2 — задачи

Рабочий файл. **Агент читает его перед началом задачи и обновляет статус в конце.**
Требования — в [r2-spec.md](./r2-spec.md).

Порядок работы — как в R1: одна задача за сессию, проверки перед коммитом,
запись в журнал решений о том, что оказалось не так, как предполагалось.

---

### R2-1 · Профиль переводчика
**Статус:** done · **Зависит от:** — · **Требования:** P1–P3

**Сделано.** Поля профиля на пользователе: имя, город, о себе, направленные
языковые пары, специализации, объединения. Страница `/profile` — своя, всегда
на правку; `/profile/:id` — карточка коллеги.

Видимость — по полям, **скрыто по умолчанию**. Наружу отдаётся не «всё, кроме
скрытого», а поимённо перечисленное: `profileView` собирает разрешённое,
а не вычёркивает запрещённое.

**Приёмка:** коллега не видит ничего сверх имени, пока владелец не открыл поле;
открытое поле не тянет за собой соседние.

### R2-2 · Записи о работе
**Статус:** done · **Зависит от:** R2-1 · **Требования:** W1–W2, W4–W5

**Сделано.** Коллекция `engagements`: событие, организатор, дата, место, режим
(синхрон, удалённый синхрон, последовательный, шушутаж), рабочие языки,
«как прошло» с подписанной шкалой и текстом, спикеры текстом. Страницы
`/experience` — список, `/experience/new` и `/experience/:id` — форма.

Связь с проектом необязательная и `ON DELETE SET NULL`: проект — подготовка,
запись — факт, и факт переживает подготовку.

### R2-3 · Команда события
**Статус:** done · **Зависит от:** R2-2 · **Требования:** W3, W6

**Сделано.** Массив `team` в записи: имя, необязательный адрес, кабина, статус.
Связь с учётной записью появляется при совпадении адреса. Запись, открытая
команде, видна связанным участникам — в списке помечена «меня назвали
в команде», открывается только на чтение.

`status` заведён сразу со всеми значениями под R2-4, чтобы подтверждение
не потребовало второй миграции.

### R2-4 · Подтверждение и оспаривание
**Статус:** done · **Зависит от:** R2-3 · **Требования:** C1–C3, C5

До этой задачи записи — личные заметки. Подтверждение превращает их
в профессиональный след, ради которого релиз и затевается.

**Сделано.** Связанный участник получает письмо и отвечает на своей странице:
подтвердить, оспорить или отозвать согласие. Решает только он сам — ни владелец
записи, ни посторонний до этих действий не дотягиваются. До ответа участие
показывается неподтверждённым; оспоренное и отозванное выпадают из команды.

### R2-5 · Приглашение коллеги из записи
**Статус:** done · **Зависит от:** R2-3 · **Требования:** C4

**Сделано.** На своей записи видны коллеги, названные с адресом, но без связи —
значит учётной записи у них нет. Кнопка зовёт их тем же механизмом, что и
администратор: одноразовая ссылка со сроком, письмо из хука коллекции.

### R2-6 · Профиль показывает записи
**Статус:** done · **Зависит от:** R2-2, R2-4 · **Требования:** P4

**Сделано.** В карточке коллеги — его записи, отобранные тем же правилом, что
и сами записи, плюс счётчик и «с какого года». И то и другое считается
по видимому смотрящему, а не по всему.

### R2-7 · Напоминание о неподтверждённом
**Статус:** done · **Зависит от:** R2-4 · **Требования:** C6

**Сделано.** Суточный крон в 9:30 пишет связанному участнику, который получил
приглашение и не ответил: через трое суток после события, один раз, не позже
двух месяцев. Правила взяты у напоминания о разборе, отличие одно — письмо
идёт не владельцу записи, а названному в ней коллеге.

---

## Журнал решений

| Дата | Задача | Что оказалось не так |
|---|---|---|
| 15.09 | R2-1 | Языки профиля шире языков генерации: тренажёр умеет то, на что есть пресеты и голоса, а переводчик работает с чем работает. Подрезать профиль под наши возможности значило бы сделать карточку неправдивой |
| 15.09 | R2-1 | Видимость — список разрешённого, а не запрещённого. При списке исключений новое поле пользователя (лимит расходов, отметка приглашения) уехало бы коллегам само, и заметить это было бы нечем. Тест сверяет набор полей карточки целиком |
| 15.09 | R2-1 | Скрыто по умолчанию. Обратный порядок однажды показал бы коллегам поле, о появлении которого человек не знал |
| 15.09 | R2-1 | Имя из списка скрываемых исключено: карточка без имени не карточка, а скрыться целиком можно, просто не заполняя профиль |
| 15.09 | R2-1 | Почта не годится в запасной вариант имени: коллега не должен узнавать чужой адрес лишь потому, что человек не вписал имя. Вместо адреса — «Без имени» |
| 15.09 | R2-1 | Схема миграции списана с той, что Payload создаёт через `push` на локальной базе, а не сочинена: у массивов свои служебные колонки (`_order`, `_parent_id`, строковый `id`), и расхождение вылезло бы только в проде. Миграция проверена на чистой базе — семь штук, схема собирается с нуля |
| 15.09 | Генерация | **Три ошибки, найденные живым использованием.** Человек сгенерировал скрипт дважды на одном проекте — и всё пошло не так. Общая причина: повторная генерация добавляла записи, не убирая прежние. У озвучки это было сделано, у скрипта нет |
| 15.09 | Генерация | Путь артефакта постоянен (`projects/3/script.md`), файл перезаписывается. Старые строки после этого указывали на новый файл, но показывали прежний размер: список врал о том, что скачаешь. Теперь прежние записи убираются |
| 15.09 | Генерация | Термины задваивались: 84 строки при 66 уникальных. В выгрузке для кабины это 18 повторов. Известные теперь пропускаются и **не переписываются**: существующий мог быть выверен человеком или добыт на событии, а это ценнее свежей догадки модели |
| 15.09 | Генерация | В имени файла показывался путь хранилища вместе со случайным суффиксом `addRandomSuffix`. Имя берётся от браузера и чистится: разделители пути и управляющие символы на экран попасть не должны |
| 15.09 | Тесты | Первый тест на имя файла оказался пустым: он шёл формой, а ошибка жила в пути загрузки через Blob. Переписан так, чтобы падать без починки — падает трижды |
| 15.09 | R2-2 | В интерфейсе это «Опыт», а не «Конференции»: у переводчика бывают суд, переговоры и сопровождение делегации, и называть это конференцией — заставлять человека врать в собственной карточке. Слово «События» тоже отпало: оно уже занято полем проекта |
| 15.09 | R2-2 | Внешний ключ на проект — `SET NULL`, а не каскад. Удаление подготовки не должно стирать запись о конференции, которая действительно была |
| 15.09 | R2-2 | У шкалы «как прошло» каждое деление подписано словами. Голые 1–5 несопоставимы: у каждого своя четвёрка, и через год собственная оценка ничего не скажет |
| 15.09 | R2-2 | Спикеры — плоский текст без единой связи, даже при совпадении имени с учётной записью. Они не пользователи сервиса, их согласия нет и не будет. Тест сверяет набор полей спикера целиком, чтобы связь не появилась незаметно |
| 15.09 | R2-2 | Негодный режим, незнакомый язык и оценка вне шкалы подменяются на разумные, а не валят сохранение: человек заполняет это после события, усталый, и терять запись из-за опечатки в форме нельзя |
| 15.09 | R2-3 | Связь только по адресу, никогда по имени: однофамильцев хватает, а ошибка приписала бы человеку чужую работу. Названный текстом чужую запись не видит — совпадение имени не основание |
| 15.09 | R2-3 | Правка записи владельцем не сбрасывает статус участника: подтверждение даёт коллега, и владелец не должен уметь снять его, поправив кабину в форме. Тест валится, если перенос статуса убрать |
| 15.09 | R2-3 | Оспоривший и отозвавший согласие выпадают из команды и теряют доступ: первый говорит, что его там не было, второй забрал согласие на упоминание. Показывать это как факт нельзя (C3, C5) |
| 15.09 | R2-3 | **Ошибка в работе:** восстановил файл через `git checkout` после проверки обратным ходом — и стёр незакоммиченную работу той же сессии. Для временных подмен годится только копия файла, а не git |
| 15.09 | R2-3 | **Ошибка, пойманная тестом восстановления.** Миграцию назвал `engagement_team`, а Payload применяет их по алфавиту: подчёркивание меньше буквы «s», и она встала РАНЬШЕ `engagements` — таблица создавалась до той, на которую ссылается. На рабочей базе не видно, потому что таблицы уже есть; вылезло только на чистой. Переименована в `engagements_team` |
| 15.09 | R2-3 | **Ошибка в работе:** закоммитил с двумя падающими тестами — `grep` в конвейере съел код возврата `npm run test`, и цепочка `&&` пошла дальше. Проверять результат прогона, а не наличие вывода |
| 15.09 | R2-4 | Статус меняет только сам участник, и меняет только свою строку. Владелец записи не дотягивается до чужого подтверждения ни действием, ни правкой формы — иначе подтверждение не значило бы ничего |
| 15.09 | R2-4 | Оспорив или отозвав согласие, человек выпадает из команды и теряет доступ к записи. Возвращать его после этого на страницу нельзя — он увидел бы 404 вместо ответа на собственное действие, поэтому редирект в список |
| 15.09 | R2-4 | `listed` → `invited` ставится только после **удавшейся** отправки письма. Не ушло — статус прежний, и напоминание (C6) попробует снова. Поставь раньше — уведомление потерялось бы навсегда |
| 15.09 | R2-4 | Отказ почты не срывает сохранение записи: запись важнее уведомления |
| 15.09 | R2-4 | Серверные действия должны быть объявлены `async` явно: стрелка, возвращающая промис, собирается локально, но валит сборку Next |
| 15.09 | R2-5 | Механизм приглашений переиспользован, а не написан второй раз. Вход — не то место, где стоит держать два похожих пути: вторая поверхность означала бы вторую поверхность для ошибок |
| 15.09 | R2-5 | Звать можно только адрес, уже названный в этой записи. Иначе форма стала бы способом рассылать приглашения кому угодно от чужого имени |
| 15.09 | R2-5 | В заметке приглашения имя приглашающего из профиля, а не его почта: адрес показывать постороннему человеку незачем |
| 15.09 | R2-5 | Уже зарегистрированному приглашение не создаётся: связь появится сама при следующем сохранении записи |
| 15.09 | R2-6 | Профиль — витрина тех же записей, а не обходной путь к ним: отбор идёт той же функцией `canSee`, никакого «в профиле показываем чуть больше» |
| 15.09 | R2-6 | Счётчик и «с какого года» считаются по видимому. «34 конференции» при двух показанных — это и есть утечка: число говорит то, чего человек показывать не собирался. Год из скрытой записи выдал бы её существование так же |
| 15.09 | R2-6 | Список подрезается, счётчик остаётся полным: он про всё видимое, а не про показанный кусок |
| 15.09 | R2-7 | Правила напоминания взяты у E5 намеренно, а не выведены заново: разойдись они в мелочи — и получились бы две рассылки с разным характером |
| 15.09 | R2-7 | Отдельный статус «напомнили»: без него письмо уходило бы каждые сутки. Ставится только после удавшейся отправки — не ушло, попробует снова |
| 15.09 | R2-7 | «Напомнили» остаётся активным участником: иначе человек получил бы письмо со ссылкой на страницу, которой уже не видит |
| 15.09 | R2-7 | **Обещание из R2-3 не сбылось.** Перечисление статусов заводилось «сразу со всеми значениями, чтобы не делать вторую миграцию» — и вторая всё равно понадобилась. Угадать полный набор заранее не вышло; закладываться на это не стоило |
| 15.09 | R2-7 | Отката у добавления значения в enum нет: PostgreSQL не умеет удалять значение, а пересоздавать тип с переносом данных ради отката — риск больше пользы. `down` намеренно пуст, и это написано в самой миграции |
| 15.09 | R2-7 | **Проверено обратным ходом, и с пользой:** отбор по статусу в запросе и проверка в цикле по отдельности не нужны — каждая покрывает тесты сама. Нужны обе: в одной записи бывает несколько участников с разными статусами, запрос вытащит её целиком. Пояснение добавлено в код, чтобы «лишнюю» проверку не выкинули |
| 15.09 | R2 | **Найдено живым использованием, сломало прод.** Добавив коллекцию, я создал её таблицы и забыл про служебную `payload_locked_documents_rels`: Payload держит там по колонке на каждую коллекцию и опрашивает их ВСЕ при любой записи. Падало сохранение профиля — при правке пользователя, а не записи о работе |
| 15.09 | R2 | **Открытие, которое обесценивает прежние проверки.** `payload migrate` локально сначала синхронизирует схему через `push`, а потом применяет миграции. Значит все прежние проверки «на чистой базе» молча чинились push'ем и миграции не проверяли вовсе. Отсюда новая проверка — статическая, по тексту миграций: она не зависит ни от базы, ни от того, что Payload делает при инициализации |
| 15.09 | R2 | Порядок в `migrations/index.ts` приведён к алфавитному и закреплён тестом: Payload применяет миграции по имени, а не в порядке массива, и расходящийся файл вводит читателя в заблуждение |
| 15.09 | R2 | `sslmode=require` от Neon переписывается в `verify-full` явно. Драйвер `pg` сегодня трактует их одинаково, но предупреждает, что в следующей мажорной версии `require` потеряет проверку сертификата: соединение останется зашифрованным, а защиты от подмены узла не будет |
| 15.09 | R2 | `/studio` удалена по просьбе владельца продукта вместе с маршрутом `/api/tts`: проекты делают то же самое, но с документом, скриптом и глоссарием. Маршрут убран не только как неиспользуемый, а как неиспользуемый и умеющий тратить деньги на Google — это хуже просто мёртвого кода. Заодно вычищены осиротевшие типы |
| 15.09 | R2 | **Проверено живьём на боевом, двумя людьми.** Владелец одной учётной записи завёл запись и назвал вторую; та связалась по адресу и подтвердила участие через три минуты. Названный без адреса остался `listed` и несвязанным — как и задумано: по имени мы не связываем. В карточке коллеги запись видна, счётчики считаются по видимому. До этого весь R2 держался на тестах: сценарий требует двух живых людей, и в одиночку он не проверяется |
| 15.09 | Аудит Б1 | Доступ Payload на создание **не умеет** фильтровать по владельцу: `Where` он вернуть даёт, но применяется тот к отбору существующих строк, а не к содержимому новой. Отсюда `create: authenticated` у пяти дочерних коллекций — и запись в чужой проект по одному лишь идентификатору. Проверка владельца перенесена в хук `beforeChange`; `create` осознанно оставлен `authenticated`, рядом комментарий, почему ужесточить его нельзя |
| 15.09 | Аудит Б1 | Правка — второй вход в ту же дыру: доступ отбирает, КАКУЮ строку менять, но не куда её переставить. Хук проверяет и создание, и правку; тест на переезд строки падает, если убрать второе |
| 15.09 | Аудит Б1 | Чужой проект отвечает «не найден», а не «нельзя». Разные ответы при переборе последовательных идентификаторов сложились бы в список чужих проектов |
| 15.09 | Аудит Б2 | Найдено при починке и в отчёте аудита отсутствовало: каскад удаления проекта сносит файлы по путям **из базы**. Подменённый путь не читал бы чужой файл, а удалял его — проверки в маршруте выдачи для этого мало, поэтому заслон стоит на записи и действует в том числе на наш собственный серверный код |
| 15.09 | Аудит Б2 | Форма пути вынесена в `src/lib/artifact-path.ts` без зависимостей: её знают и запись, и проверка. Продублированная строка разошлась бы ровно тогда, когда проверка перестанет ловить подмену. Одного `startsWith` мало — `projects/7/../8/script.md` начинается правильно; проверяется и отсутствие вложенности, и завершающий слэш (иначе проект 7 забрал бы файлы проекта 70) |
| 15.09 | Аудит | Каждый из трёх заслонов выломан по отдельности, и на каждый падают свои тесты: 6 на владельца проекта, 3 на путь при записи, 1 на путь в маршруте. Строка с кривым путём заводится запросом прямо в базу — через Payload она больше не создаётся вовсе, и это ровно то, от чего защищается маршрут: не от того, что мы умеем записать сегодня, а от строки, которая окажется в базе завтра |
| 15.09 | Аудит Б3 | **Пункт аудита оказался неверным, и ошибка моя.** Из того, что `DATABASE_URL` выдан обоим окружениям, я заключил, что превью работает с боевой базой, — и не проверил, к чему сборка подключается на самом деле. Интеграция Neon заводит превью свою ветку и подставляет её поверх переменной: отпечатки соединений в логах разные (превью `4ac9e26e`, боевое `23ffbffc`). Область видимости переменной в панели не отвечает на вопрос, куда подключается приложение; отвечает только запуск. Отпечаток в логах заведён раньше именно для этого, и я им не воспользовался |
| 15.09 | Аудит Б3а | Разделение баз, принятое за хорошую новость, само создало проблему: хранилище Blob одно на оба окружения, а счётчики идентификаторов разошлись. Проект 4 на стенде и проект 4 на боевом — разные проекты с одним путём `projects/4/script.md`. Лечится приставкой окружения; у боевого приставки нет намеренно — пути уже в базе, и менять их задним числом значит потерять файлы |
| 15.09 | Аудит Б3б | `!secret || заголовок совпал` в кроновых маршрутах означало, что **отсутствие** секрета открывает маршрут. На превью секрета нет, а база там — ветка от боевой с настоящими данными. Спасал только вход Vercel, то есть защита снаружи, выключаемая галочкой. Теперь без секрета открыто лишь вне облака: в развёртывании его отсутствие — ошибка настройки и обязано читаться как запрет |
| 15.09 | Аудит Б4 | Доступ в админку у Payload объявляется **не** настройкой `admin` в конфигурации, а функцией `access.admin` в самой коллекции, названной в `admin.user`. В отчёте аудита я написал первое; правильным оказалось второе. Мелочь, но ровно та, из-за которой «починка одной строкой» не чинит ничего |
| 15.09 | Аудит Б4 | Роль проверяется в коллекции, а не в `src/proxy.ts`: в куке лежит только идентификатор, а поход в базу из прокси на каждый запрос — плата за то, что Payload проверит и сам. Прокси остаётся грубым ситом: без подписанной куки разметку админки не отдаём |
| 15.09 | Аудит Б4 | Отказ Payload показывает по-английски и предлагает «Log out» — то есть выход из всего приложения. Оставлено: ссылок на `/admin` в интерфейсе нет, попасть туда можно только набрав адрес. Если админка когда-нибудь появится в меню, это надо будет переделать раньше |
